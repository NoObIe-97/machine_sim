[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "machine-sim"
version = "0.1.0"
description = "After Silicon: Bias-Guarded Machine Civilization Emergence Simulator"
requires-python = ">=3.11"
dependencies = ["click>=8.0"]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov>=4.0"]

[project.scripts]
machine-sim = "machine_sim.cli.main:cli"

[tool.pytest.ini_options]
testpaths = ["machine_sim/tests"]
pythonpath = ["."]

[tool.coverage.run]
source = ["machine_sim"]
omit = ["machine_sim/tests/*", "machine_sim/analysis/*"]

[tool.coverage.report]
show_missing = true
fail_under = 80
